# FocusFlow Release Checklist

## Required

1. `cmd /c npm run lint`
2. `cmd /c npm run build`
3. Focused manual smoke test for touched behavior

## Optional

1. Validate import/export when data layer is touched
2. Validate drag/drop flows when board/task movement logic is touched
3. Validate Electron app startup path when desktop layer is touched